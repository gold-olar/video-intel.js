/**
 * Example test file
 * This is a placeholder to ensure the test infrastructure works
 */

describe('VideoIntel Setup', () => {
  it('should pass basic test', () => {
    expect(true).toBe(true);
  });

  it('should have correct environment', () => {
    expect(typeof window).toBe('object');
  });
});

